import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proglove-experience-day-task',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
